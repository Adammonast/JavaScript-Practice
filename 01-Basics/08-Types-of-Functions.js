// Function that calculates a value
// takes a parameter
// use return keyword to return value
function square(number) {
  return number * number;
}

// pass the square function into the log function to display value of argument
console.log(square(3));
